spring:
  application:
    name: rx-smoker
  datasource:
    url: jdbc:h2:mem:smoker;DB_CLOSE_ON_EXIT=FALSE
    username: smoker
    password: smoker

leader:
  election:
    lock-key: services/rx-smoker/leader
    self-id: je suis le roi
    polling-delay: 10000 # milliseconds
    checks:
      - service:rx-smoker
      - serfHealth
