spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: false
        service-name: ${spring.application.name}
      config:
        prefix: services
        data-key: config
        profile-separator: ':'
        format: yaml

management:
  security:
    enabled: false
