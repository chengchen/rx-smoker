spring:
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: false
        instanceId: ${spring.application.name}:${random.value}
      config:
        prefix: services
        data-key: config
        profile-separator: ':'
        format: yaml

management:
  security:
    enabled: false
